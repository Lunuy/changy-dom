import { Array } from "changy";
import Node from "../Node/Node";
import * as ChangyDom from "../index"

function Fragment(props : any, childs : Array<Node>) {
    return childs;
}

export default Fragment;